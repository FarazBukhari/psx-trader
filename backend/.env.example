# PSX Signal System — Backend Environment Variables
# Copy this to .env and edit as needed

# Poll interval in seconds (how often to scrape PSX)
# 30s recommended — halves DB write frequency vs the original 15s default
PSX_POLL_INTERVAL=30

# Set to "true" to use mock data (skips live scraping)
PSX_MOCK=false
